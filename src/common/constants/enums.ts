export enum RoleValues {
  USER = 'user',
  ADMIN = 'admin',
  DELIVERER = 'deliverer',
}
export enum OrderStatues {
  WAITING = 'waiting',
  PICKED = 'Picked',
  CANCELLED = 'Cancelled',
  DELIVERED = 'Delivered',
}
